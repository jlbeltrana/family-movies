# Copia a .env y rellena. No subas .env al repo.
# Para producción, configura estos valores en Google Cloud Console (Cloud Functions) o con:
#   firebase functions:secrets:set JWT_SECRET
#   (WORKER_BASE_URL se puede definir en la consola como variable de entorno)

WORKER_BASE_URL=https://family-movies-worker.TU_SUBDOMINIO.workers.dev
